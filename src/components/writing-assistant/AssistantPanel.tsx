import * as React from "react"; import { WandSparkles } from "lucide-react"; import { Alert, AlertDescription } from "@/components/ui/alert"; import { Badge } from "@/components/ui/badge"; import { Button } from "@/components/ui/button"; import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"; import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue, } from "@/components/ui/select"; import { Separator }from "@/components/ui/separator"; import { Switch } from "@/components/ui/switch"; import { Textarea } from "@/components/ui/textarea"; import { excerpt } from "./editor-utils"; import type { AssistantRequest, Intent, LengthPreference, Tone, } from "./types"; import type { BaselineDocument } from "./types"; interface AssistantPanelProps { selectedText: string; documentText: string; baselines: BaselineDocument[]; onCreateProposal: (request: AssistantRequest) => void; } const AssistantPanel = ({ selectedText, documentText, baselines, onCreateProposal, }: AssistantPanelProps) => { const [intent, setIntent] = React.useState<Intent>("rewrite"); const [tone, setTone] = React.useState<Tone>("professional"); const [length, setLength] = React.useState<LengthPreference>("same"); const [customPrompt, setCustomPrompt] = React.useState(""); const [useBaseline, setUseBaseline] = React.useState(false); const [outlineText, setOutlineText] = React.useState(""); React.useEffect(() => { setUseBaseline(baselines.length > 0); }, [baselines.length]); const baselineText = baselines.map((document) => `### ${document.name}\n${document.text}`).join("\n\n"); const selectedWordCount = selectedText.trim().split(/\s+/).filter(Boolean).length; const canGenerate = intent !== "custom" || customPrompt.trim().length > 0; const handleGenerate = () => { onCreateProposal({ intent, tone, length, customPrompt: customPrompt.trim(), selectedText, documentText, baselineText, useBaseline: useBaseline && baselines.length > 0, }); }; const generateChapter = () => { if (!outlineText.trim()) return; const request: AssistantRequest = { intent: "custom", customPrompt: "Generate a chapter based on this outline: " + outlineText, selectedText: outlineText, documentText: "", tone, length, useBaseline: false, }; handleCreateProposal(request); }; const handleCreateProposal = React.useCallback((request: AssistantRequest) => { // If intent is "chapter", treat as custom with outline let finalRequest = request; if (request.intent === "chapter") { finalRequest = { ...request, intent: "custom", customPrompt: "Generate a chapter based on this outline: " + (request.selectedText || outlineText), selectedText: outlineText, documentText: "", useBaseline: false, }; } const generatedProposal = generateAssistantProposal(finalRequest); setProposal(generatedProposal); setReviewOpen(true); showSuccess("Proposal ready. Review it before applying."); }, []); const handleSelectionChange = React.useCallback((nextSelection: EditorSelectionState) => { setSelection(nextSelection); }, []); const handleEditorCommand = React.useCallback((command: EditorCommand) => { editorRef.current?.runCommand(command); }, []); // ... rest of the component unchanged ... return ( <Card className="overflow-hidden border-emerald-100 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-950"> <CardHeader className="pb-3"> <div className="flex items-start justify-between gap-3"> <div className="flex items-center gap-3"> <div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-emerald-50 text-emerald-600 dark:bg-emerald-950 dark:text-emerald-300"> <WandSparkles className="h-5 w-5" /> </div> <div> <CardTitle className="text-lg">Prompt-first assistant</CardTitle> <CardDescription> Choose the outcome, review a proposal, then approve where it goes. </CardDescription> </div> </div> </CardHeader> <CardContent className="space-y-4"> {/* existing sections */} {/* New Chapter Outline section */ <div className="space-y-2"> <label className="text-sm font-semibold text-slate-900 dark:text-white"> Chapter Outline </label> <Textarea value={outlineText} onChange={(e) => setOutlineText(e.target.value)} placeholder="Enter chapter outline (one paragraph per line or numbered list)" className="min-h-28 rounded-2xl" /> <Button type="button" className="w-full rounded-2xl bg-emerald-600 font-semibold text-white shadow-sm hover:bg-emerald-700" onClick={generateChapter} disabled={!outlineText.trim()}> Generate Chapter </Button> </div> {/* existing Button and other UI */ <Button type="button" variant="secondary" className="w-full rounded-2xl" onClick={handleNewDocument} > Start a fresh document </Button> </CardContent> </Card> ); export default AssistantPanel;